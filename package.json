{
  "name": "trueauth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"BACKEND,FRONTEND\" -c \"cyan.bold,yellow.bold\" \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
